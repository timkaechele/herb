import dedent from "dedent"
import { describe, test } from "vitest"
import { <%= ruleClassName %> } from "../../src/rules/<%= ruleName %>.js"
import { createLinterTest } from "../helpers/linter-test-helper.js"

const { expectNoOffenses, expectError, expectWarning, assertOffenses } = createLinterTest(<%= ruleClassName %>)

describe("<%= ruleClassName %>", () => {
  test("valid case TODO", () => {
    expectNoOffenses(dedent`
      <h1>
        <%%= title %>
      </h1>
    `)
  })

  test("invalid case TODO", () => {
    expectError("TODO: update rule message")

    assertOffenses(dedent`
      <h1>
        <%%= title %>
    `)
  })
})
