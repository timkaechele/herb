#ifndef ERBX_AST_PRETTY_PRINT_H
#define ERBX_AST_PRETTY_PRINT_H

#include "ast_nodes.h"
#include "buffer.h"

void ast_pretty_print_node(AST_NODE_T* node, size_t indent, size_t relative_indent, buffer_T* buffer);

#endif
